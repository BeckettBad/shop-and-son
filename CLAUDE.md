# CLAUDE.md

@AGENTS.md

Claude Code reads this file, not AGENTS.md. The `@AGENTS.md` import above pulls in the single source of
truth so Claude and Codex follow the same rules without maintaining two copies (they used to be identical
duplicates, which drift). Approve the import once when Claude Code asks.

Put Claude-only notes below this line. Everything shared goes in AGENTS.md.
